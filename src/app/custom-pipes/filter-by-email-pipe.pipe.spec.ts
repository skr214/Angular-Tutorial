import { FilterByEmailPipePipe } from './filter-by-email-pipe.pipe';

describe('FilterByEmailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByEmailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
